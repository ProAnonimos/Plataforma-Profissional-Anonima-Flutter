import 'package:flutter/material.dart';

class NoticiasTecnologiaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Notícias de Tecnologia")),
      body: Center(child: Text("Conteúdo das notícias aqui")),
    );
  }
}
